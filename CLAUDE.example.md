# CLAUDE.example.md

훅이 쌓은 것을 **읽고 쓰는 쪽 규칙**이다. `settings.example.json` 이 훅을 배선한다면
이 파일은 그 산출물을 어떻게 다룰지를 정한다. 아래를 `~/.claude/CLAUDE.md` 에 붙여넣고
`<vault>` 를 실제 vault 경로(절대경로)로 바꾼다.

훅만 설치하고 이 규칙을 넣지 않으면 **기록은 쌓이는데 아무도 읽지 않는 상태**가 된다.
세션 시작 시 자동 주입이 없기 때문이다(단발 질문 세션까지 매번 수천 토큰을 물어 폐기했다).

---

## 진행 중 작업 조회

사용자가 **이어서 할 일·진행 상황을 묻거나, 기존 주제를 이어서 작업하려 하면**
`<vault>/INDEX.md` 를 읽는다. 그 외의 요청이면 읽지 않는다.

- `INDEX.md` 는 목차다 — 주제별 제목·상태·다음 한 줄. 자동 생성물이라 **직접 편집하지 않는다.**
- 목록의 번호(`5.`, `5-2`)는 **그 시점 렌더의 순번**이다. "5-2 이어서 하자" 같은 지시를 받으면
  `INDEX.md` 를 읽어 항목을 찾고 **무슨 작업인지 제목을 말한 뒤** 시작한다 — 잘못 짚었으면 그 자리에서 드러난다.
  번호는 다음 렌더에 밀리므로 계획서·주제 파일 등 남는 문서에는 쓰지 않는다.
- 정본은 `<vault>/topics/<slug>.md` 다.
  `📌 결론`(이미 아는 사실) · `❌ 접은 안`(이미 기각한 대안) · `📈 진행 로그` · `🔜 다음`.
- 설계 문서 경로는 그 파일 frontmatter 의 `plan:` 에 절대경로로 있다. 이어서 설계를 논의할 때만 읽는다.
- 기록은 SessionEnd 훅이 자동으로 한다. **세션 도중에 vault 에 직접 쓰지 않는다.**

## 기획 시 주제 바인딩

기획을 시작할 때 계획서 경로와 함께 **대응 주제 슬러그**를 정한다(`<vault>/topics/<slug>.md`).
기존 주제면 그것을 쓰고, 없으면 사용자에게 신규 생성 여부를 묻는다 — **훅은 새 주제를 만들지 않는다.**

주제 파일 frontmatter 에 `plan: <계획서 절대경로>` 를 넣으면 `INDEX.md` 목차에
"먼저 이 파일을 읽어라"는 지시문으로 실린다. 다음 세션에서 이어서 할 일을 물으면
`INDEX.md` 를 읽는 순간 계획서까지 따라 읽힌다. 상대경로는 쓰지 않는다 — `plans/` 는 여러 곳에 있다.

## 결정의 이관

`plans/*.md` 는 작업대(단발성)이고 `topics/<slug>.md` 가 정본이다. 이관하지 않으면
계획서를 정리하는 순간 결정이 사라진다.

세션 종료 훅이 `📌 결론`·`❌ 접은 안` 에 자동 append 하므로 **평소에는 놔두고**,
작업이 끝날 때 계획서 전체를 훑어 훅이 놓친 항목만 보강한다.

## 태스크

`INDEX.md` 가 목차이자 할 일의 정본이다. 주제 아래로 접혀 `5-2` 처럼 번호가 붙는다.
태스크를 체크하면 완료로 내려가고, **주제(`5.`)를 체크하면 그 주제가 닫힌다**(`status: done`).

1. 주제 밖 단발 작업 — `topics/` 에 주제가 없어 갈 곳이 태스크뿐인 것
2. 주제 안이지만 순서 밖이라 잊기 쉬운 일
3. 완료 날짜를 남겨야 하는 일 — 완료 이력이 남는 경로는 태스크뿐이다

주제의 다음 단계는 `🔜 다음` 이 담는다. 순서가 있고 서로 의존하는 단계를 체크박스로 쪼개지 않는다.
**완료 체크와 삭제는 사람만 한다** — 훅은 항목을 지우거나 완료로 바꾸지 않는다.

남은 일은 전부 태스크로 남는다 — 주제의 다음 단계도 포함한다. 순서대로 체크해 나가면 되고,
체크박스여야 완료 이력이 남는다. 그 자리에서 끝난 일이면 `#완료` 를 치면 억제된다.
